{
    "name": "rag-api",
    "version": "1.0.0",
    "description": "RAG API Worker for Cloudflare Demo",
    "main": "src/index.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}